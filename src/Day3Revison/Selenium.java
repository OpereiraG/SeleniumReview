package Day3Revison;

public class Selenium {
    public static void main(String[] args) {

        //input[contains(@placeholder,'Enter Email')]
        //h3[contains(text(),'Useful Links for learning')]
        //input[@*='userId']// You don't need to mention an attribute
        //a[starts-with(text(), 'Amazon')]// filters the search by what the link starts with
        //*[local-name()='svg']//*[name()='g']// Exception in case of svg ONLY attribute

    }
}
